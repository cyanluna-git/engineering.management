---
description: FastAPI REST API conventions for Edwards Operation Board
paths: ["backend/app/api/**", "backend/app/schemas/**"]
---

# API Conventions

## Endpoint Pattern

Thin controller — delegate to service:

```python
@router.get("/", response_model=list[UserResponse])
async def list_users(
    skip: int = 0,
    limit: int = 10,
    db: Session = Depends(get_db),
    _: str = Depends(require_role(["ADMIN", "PM"]))
):
    service = UserService(db)
    return service.list(skip=skip, limit=limit)
```

## Schema Triplet Pattern

Every entity needs Create, Update, Response schemas:

```python
class UserBase(BaseModel):
    name: str = Field(..., min_length=1, max_length=100)
    email: str = Field(..., pattern=r"^[^@]+@[^@]+\.[^@]+$")

class UserCreate(UserBase):
    password: str = Field(..., min_length=8)

class UserUpdate(BaseModel):
    name: str | None = None
    email: str | None = None

class UserResponse(UserBase):
    id: str
    created_at: datetime
    updated_at: datetime

    class Config:
        from_attributes = True
```

## REST Conventions

| Method | Path | Status |
|--------|------|--------|
| GET | `/resource` | 200 |
| POST | `/resource` | 201 |
| GET | `/resource/{id}` | 200 |
| PUT/PATCH | `/resource/{id}` | 200 |
| DELETE | `/resource/{id}` | 204 |

## Error Handling

Use typed exceptions:
```python
class ResourceNotFound(HTTPException):
    def __init__(self, resource: str, resource_id: str):
        super().__init__(
            status_code=404,
            detail=f"{resource} with id {resource_id} not found"
        )
```

## Router Registration

Register in `app/main.py`:
```python
app.include_router(users.router)
app.include_router(projects.router)
```
